# JSON Flowchart Example

Vizlayer can emit Mermaid flowcharts from a constrained JSON contract.

The initial shape focuses on:

- named nodes
- directed edges
- optional edge labels
- one explicit flow direction

This narrow contract keeps the first version predictable for AI-generated visualization flows.
